name: Test EDT

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: self-hosted
    steps:
      - name: Test via bat file
        shell: cmd
        run: |
          C:\test_edt.cmd
          echo Exit code: %ERRORLEVEL%
